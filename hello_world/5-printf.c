#include <stdio.h>

/**
 *main - print sent "with proper grammar, but the outcome is a piece of art,"
 *Return: 0 for correct value, nothing for incorrect value
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
